-- 코드를 입력하세요
SELECT ins.NAME, ins.DATETIME
FROM (SELECT *
      FROM ANIMAL_INS  ORDER BY DATETIME) ins FULL OUTER JOIN ANIMAL_OUTS outs 
ON  ins.ANIMAL_ID = outs.ANIMAL_ID 
WHERE outs.DATETIME is null AND ROWNUM < 4;
